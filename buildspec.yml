version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies (none needed for HTML site)"
  build:
    commands:
      - echo "Build step - HTML site ready"
artifacts:
  files:
    - '**/*'
